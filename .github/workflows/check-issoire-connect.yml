name: Check Issoire Connect
on:
  push:
    branches: ["main","issoire-connect-launch","issoire-connect-dev-complete"]
  pull_request:
    branches: ["main"]
concurrency:
  group: issoire-connect-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Static validation
        run: |
          test -f issoire-connect/index.html
          test -f issoire-connect/app/index.html
          test -f issoire-connect/app/manifest.webmanifest
          test -f issoire-connect/app/sw.js
          for f in claim-patch.js pro-edit-patch.js search-patch.js follow-patch.js resident-patch.js job-patch.js community-patch.js deals-patch.js deal-studio-patch.js payment-connect-patch.js favorites-page-patch.js pro-alerts-classifieds-patch.js moderation-patch.js push-patch.js; do
            test -f "issoire-connect/app/$f"
            node --check "issoire-connect/app/$f"
            grep -q "$f" issoire-connect/app/index.html
            grep -q "$f" issoire-connect/app/sw.js
          done
          grep -q "addEventListener('push'" issoire-connect/app/sw.js
          grep -q "addEventListener('notificationclick'" issoire-connect/app/sw.js
          for f in v3-data-0.txt v3-data-1.txt v3-data-2.txt v3-data-3.txt; do test -f "issoire-connect/app/$f"; done
          test -f tests/issoire-connect-audit.mjs
          node --check tests/issoire-connect-audit.mjs
          node -e "JSON.parse(require('fs').readFileSync('issoire-connect/app/manifest.webmanifest','utf8'))"
          python3 - <<'PY'
          import re,pathlib
          s=pathlib.Path('issoire-connect/app/index.html').read_text()
          js='\n'.join(re.findall(r'<script>([\s\S]*?)</script>',s))
          pathlib.Path('/tmp/ic.js').write_text(js)
          PY
          node --check /tmp/ic.js
          test $(wc -c < issoire-connect/index.html) -lt 30000
          test $(wc -c < issoire-connect/app/index.html) -lt 30000
      - name: Install Chromium
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Start local server
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 >/tmp/ic-http.log 2>&1 &
          for i in {1..20}; do curl -fsS http://127.0.0.1:4173/issoire-connect/ >/dev/null && exit 0; sleep .5; done
          cat /tmp/ic-http.log; exit 1
      - name: Chromium smoke test local V3
        run: node tests/issoire-connect-smoke.mjs
      - name: Comprehensive functional audit local V3
        run: node tests/issoire-connect-audit.mjs
      - name: Wait for public GitHub Pages V3 loader
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          URL='https://djerhemiank-source.github.io/signal-deal/issoire-connect/app/index.html'
          for i in {1..60}; do
            BODY=$(curl -fsSL -H 'Cache-Control: no-cache' "$URL?deploy=${GITHUB_SHA}" 2>/dev/null || true)
            if echo "$BODY" | grep -q 'v3-data-0.txt' && echo "$BODY" | grep -q 'payment-connect-patch.js' && echo "$BODY" | grep -q 'favorites-page-patch.js' && echo "$BODY" | grep -q 'pro-alerts-classifieds-patch.js' && echo "$BODY" | grep -q 'moderation-patch.js' && echo "$BODY" | grep -q 'push-patch.js'; then
              echo 'Full Issoire Connect V3 loader is live.'
              exit 0
            fi
            echo "Waiting for full GitHub Pages V3 ($i/60)..."
            sleep 5
          done
          echo 'The full V3 loader did not appear within 5 minutes.'
          exit 1
      - name: Chromium smoke test public V3
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          IC_BASE: https://djerhemiank-source.github.io/signal-deal/issoire-connect/
        run: node tests/issoire-connect-smoke.mjs
