name: Temporary billing health check

on:
  push:
    branches: ["main"]
    paths: [".github/workflows/billing-health-temp.yml"]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check Signal Deal billing env flags
        run: |
          curl -fsS https://eazukvtjxeirbitukueb.supabase.co/functions/v1/signal-deal-billing-health
